// packages/frontend/src/components/web-components/index.ts

// Import all web components
import './article-preview';

// Export a function to ensure components are registered
export function registerWebComponents() {
  // This function just needs to be imported somewhere to ensure components are registered
  console.log('Web components registered');
}